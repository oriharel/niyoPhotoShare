<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/archivedFolderCard"
    android:layout_width="170dp"
    android:layout_height="100dp"
    android:orientation="vertical"
    app:cardCornerRadius="4dp">

    <FrameLayout
        android:id="@+id/archived_folder_event_frame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/archived_folder_event_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/preview_description"
            android:scaleType="centerCrop"
            app:srcCompat="@mipmap/pic5" />

        <TextView
            android:id="@+id/archived_folder_event_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_marginLeft="@dimen/cardview_default_elevation"
            android:layout_marginRight="@dimen/cardview_default_elevation"
            android:background="@color/bg_card_folder_title"
            android:paddingBottom="10dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingTop="10dp"
            android:text="TextView"
            android:textColor="@android:color/white"
            android:textSize="14sp" />

    </FrameLayout>

</android.support.v7.widget.CardView>